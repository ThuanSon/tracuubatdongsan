import { Rental } from "./ForRental";

export const Post = () => {
    return (
        <div
        style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center'
        }}
        >
            <Rental/>
        </div>
    );
}