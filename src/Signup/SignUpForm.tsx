import { Container, Grid, TextField } from "@mui/material"
import React from "react"

export const SignUpForm = () => {
    return (
        <Container>
            <form>
                <Grid>
                    hello   
                </Grid>
            </form>
        </Container>
    );
}